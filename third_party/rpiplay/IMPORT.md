# Bundled RPiPlay Snapshot

This folder is a local source snapshot copied into the project so the app no longer depends on:

- `/Users/horatiubudai/Downloads/RPiPlay-master`

Current source origin:

- `RPiPlay`
- copied from the local machine on `2026-03-29`

This is a source bundle only. It is not yet wired into the Android NDK build as a working renderer.
